from user_generator import generate_random_users
from movie_generator import generate_random_movies

if __name__ == "__main__":
    # Generate users and movies
    # generate_random_users(num_users=1000)
    generate_random_movies(num_movies=1000000)